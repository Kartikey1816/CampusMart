PORT=5001
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>/<database>
JWT_SECRET=5f0757d67ec1fa4fb05a1f36dd20588bf8ccd6253bfc6060677908a291260b5c
JWT_EXPIRES_IN=7d
# Comma-separated. Signups from every other domain are rejected.
ALLOWED_COLLEGE_EMAIL_DOMAINS=college.edu
CLIENT_URL=http://localhost:5173
NODE_ENV=development
# Development only: a verified account is seeded on every backend startup.
DEV_TEST_PASSWORD=12345678
# Optional. Defaults to admin@the-first-allowed-college-domain.
# DEV_TEST_EMAIL=admin@college.edu
# Required only when sending real verification emails (always required in production).
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
EMAIL_FROM="College Marketplace <no-reply@college.edu>"
